import { LoginTemplate } from "@/components/templates/login-template";

export const metadata = {
  title: "Sign In — Dars",
  description: "Sign in or create a Dars account to get your API key.",
};

export default function LoginPage() {
  return <LoginTemplate />;
}
